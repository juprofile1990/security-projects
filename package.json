{
  "name": "take-home",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,ui -c blue,magenta \"npm run dev --prefix security-incident-api\" \"npm run dev --prefix security-incident-dashboard\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
