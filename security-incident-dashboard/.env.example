# Used by `npm run dev` (see `.env.development`) and can match production API URL
# VITE_API_URL=https://security-projects.onrender.com

# To use a local API while developing, create `.env.development.local` with:
# VITE_API_URL=http://localhost:3000
